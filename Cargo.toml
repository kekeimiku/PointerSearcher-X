[workspace]
resolver = "2"
members = ["vmmap", "ptrsx", "inject", "scanner", "dumper", "ffi", "conv"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
debug = false
